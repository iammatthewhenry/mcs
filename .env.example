# Payload CMS (optional; used by /projects and to store contact submissions)
# Uncomment and fill when your Payload instance is ready:
# NEXT_PUBLIC_PAYLOAD_URL=https://your-payload-domain.com
# PAYLOAD_URL=https://your-payload-domain.com
# PAYLOAD_API_KEY=your-api-key-here

# SES intentionally disabled for now — no AWS keys required.
